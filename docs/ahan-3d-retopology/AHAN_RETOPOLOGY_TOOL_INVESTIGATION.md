# 阿韩重拓扑工具调研报告（路线3：调查候选方案，未安装/未执行）

- **前置文档**：[AHAN_3D_MODEL_AUDIT.md](AHAN_3D_MODEL_AUDIT.md)、[AHAN_RETOPOLOGY_PLAN.md](AHAN_RETOPOLOGY_PLAN.md)（第十一节记录了2A/2B/2C的执行结果与QuadriFlow排查过程）
- **本文档性质**：纯调研，未安装任何软件，未执行任何新的重拓扑操作。
- **调研目标**：不是"把三角面变成四边面"，而是找到能产出**真正适合骨骼动画的方向性Edge Flow**（沿肢体走向的边线环）的方法，尤其是眼周、嘴周、颈、肩、肘、腕、功能性髋、膝这8个区域。

---

## 〇、重要背景发现：服务器上已存在相关历史工作

在调研过程中，发现 `/home/yangzhen/Edints/blender/a-han/` 目录下已经有两组**独立于本次会话**的既有工作，与本任务高度相关，一并汇报：

### 0.1 `rig-v01/`：直接在原始500K高模上蒙皮的绑骨尝试

- 已经把 `AHAN_Hunyuan_Master_500K.glb`（原始高模，只读锁定保存在隐藏集合 `AHAN_SOURCE` 中，未被覆盖）复制到工作副本并完成了一次完整绑骨：25根变形骨（含双耳、三根头发、尾巴独立骨骼）+ IK/FK双臂双腿控制骨，并做出了21个动作（待机、眨眼占位、点头摇头、挥手、抬手、指向、鞠躬、走路、坐下、开心、思考、梳头测试等）。
- 由于原始网格3,501个断开岛+96,950条非流形边，标准骨骼热力学自动权重（Bone Heat）失败，改用了一套"世界坐标连续包络"的自定义权重方案（同一空间区域内的碎片获得一致权重，避免撕裂）——这是一个**绕开重拓扑、直接在坏拓扑上打补丁**的权宜方案。
- 报告原文明确写道："50万面高模适合作为母模和离线渲染，不适合直接作为网页实时动画资产""后续建议从此母模做受控重拓扑"——**这份既有工作本身就在等待我们现在做的这件事**，两者目标一致、不冲突。
- 报告也指出："当前Rig优先验证轻柔小幅动作；极端动作可能暴露断开网格岛之间的缝隙""眼睛、徽章、衣服、头发并非独立对象……应在不改变外观的前提下建立干净动画拓扑"——与我们的审计结论完全吻合。
- **对本次调研的价值**：里面的"估算关键点"坐标（头部中心、身体中心、肩部±0.25,0,0.60、手部±0.345,-0.075,0.255、胯部0,0.01,0.28、脚部±0.16,-0.06,0.05、尾巴0,0.31,0.29、耳朵±0.30,0,0.93）可以和我们第一阶段自己测出的高度带交叉核对——多数吻合，但"手部"位置比我们第一阶段测的更低（他们给出z=0.255，约23%高度，我们測的手腕/手部聚簇约35-58%高度），这个差异建议在后续实际重拓扑建模阶段用高模现场核实，不在本次调研中强行下结论。

### 0.2 `a-han-head-master-v1/v2/v3` + `create_a_han_head_master.py`：独立的程序化头部重建

- 这是一套完全独立的Python脚本，用数学公式（椭球+профиль函数）**从零参数化搭建**了一个阿韩头部（v1→v2→v3三个迭代版本），不是对真实高模的重拓扑或采样。
- 实际查看 `checks/head-v3/head-front.png` 渲染结果：整体神似但脸部奶油色色块边界有明显的锯齿/阶梯状硬边（很可能是简单布尔或顶点网格拼接产生），细节精度和真实阿韩有肉眼可见差距。
- **结论**：这条路径不满足本任务"必须保持阿韩现有外观不变"的Design Lock要求（它是重新诠释而非精确复刻），**不建议**作为本次重拓扑的基础，仅作为背景信息记录。

---

## 一、候选方案逐一调研

### 方案A：Instant Meshes

- **是什么**：牛津/加州理工出品的开源自动重拓扑工具，基于方向场（orientation field）+位置场（position field）联合优化，是目前开源界公认在"产出跟随曲面走向的四边面流"这件事上做得最好的工具之一；支持**交互式笔刷手动引导边线走向**（这正是我们需要的"人工引导关键区域Edge Flow"能力）。
- **本机现状**：未安装，本地未找到二进制文件；`apt-cache` 中没有现成包；官方也不提供pip包。
- **获取方式**：GitHub仓库 `wjakob/instant-meshes` 可访问（已确认服务器能连外网），但仓库自2021年后基本停止维护，预编译的Linux二进制是多年前构建的，与当前Ubuntu的glibc/Qt版本兼容性存在风险；从源码编译需要 CMake + Eigen + TBB + Qt + NanoGUI 等依赖链，构建工作量不小，且GUI依赖较重（它主要设计为交互式桌面工具）。
- **命令行/自动化能力**：**支持**。官方提供纯命令行模式（不开界面），可指定输入网格、目标面数、是否强制四边面、是否使用已保存的方向场/位置场约束文件等，理论上可以脚本化批量调用。但"手动引导边线"这个它最大的优势功能本质上依赖交互式笔刷，命令行模式下要复现同等效果，需要预先算好约束数据喂给它，等于还是要人工介入设计阶段。
- **保持外观**：可以——工作流是"以现有高模为参照生成新拓扑"，配合后续Shrinkwrap投射回高模表面，外观保真度取决于我们给的目标面数和方向场质量，原理上支持很好地保外观。
- **对8个关键区域的改善能力**：**这是其相对优势**——方向场算法本身会自然沿曲率走向产生较为合理的边线环，但阿韩是浑圆无尖锐特征的Q版角色，肩、肘、腕、髋、膝这些位置在几何上并没有明显的曲率突变（不像真人有骨点棱角），纯自动方向场**不保证**恰好在功能性关节位置放环，大概率仍需要人工引导曲线（Instant Meshes支持画"curve"约束边线走向和位置）来钉住这8个区域。
- **风险**：编译/兼容性不确定；即使编译成功，"引导曲线"要通过命令行复现需要额外脚本准备约束数据，工作量比想象中大。

### 方案B：Blender 内置 QuadriFlow（已实测，问题已记录）

- 已在阶段2B详细尝试，遇到持续性的"网格必须是流形"报错且排查不出根因（详见方案对比总表下方的详细记录，已写入 `AHAN_RETOPOLOGY_PLAN.md` 第十一节）。
- **命令行/自动化能力**：`bpy.ops.object.quadriflow_remesh()`，纯脚本可调用，是目前唯一"本机已装好、真正支持方向性四边面流、且是Blender原生功能不需要额外安装"的方案——但目前在我们的模型上跑不通。
- **值得记录的是**：本次调研没有发现明确证据证明这是"模型问题"而非"这套Blender构建的QuadriFlow实现缺陷"；如果之后有机会用不同的Blender版本（比如换一个官方标准发行版而非当前这个可能经过定制的构建）重新测试，不能排除能跑通的可能性——这是一个相对低成本、值得在正式投入方案A或方案D之前快速复测一次的选项（复测本身不需要安装新软件，只需要另一份Blender二进制，如果之后能获取的话）。

### 方案C：MeshLab / pymeshlab

- **本机现状**：`meshlab` 在apt源里能查到但未安装；`pymeshlab`（2025.7.post1）在PyPI上能查到版本、网络可达，理论上 `pip install` 可用（本次未执行安装）。
- **命令行/自动化能力**：pymeshlab是纯Python库，天然适合脚本化批量调用，自动化程度最高、集成最简单。
- **四边面/方向性流能力**：**这是它的短板**——MeshLab的核心重网格化能力是"各向同性显式重网格化"（Isotropic Explicit Remeshing，产出均匀分布的三角面，不带方向场）和一个简单的"转四边面"过滤器（本质上和Blender自带的Tris to Quads类似的启发式合并，不是QuadriFlow/Instant Meshes级别的方向场算法）。也就是说，即使装上它，大概率得到的还是和我们阶段2B已经试过、已经判定不合格的"无方向性四边面拼接"类似的结果，**不太可能解决Edge Flow这个核心诉求**。
- **结论**：自动化程度最高但达不到目标质量，作为主力方案不推荐；可以作为"网格清理/预处理"的辅助工具（比如更彻底的按距离合并、法线修复），但不建议指望它解决方向性流的问题。

### 方案D：Blender 内置手工/半手工重拓扑（Poly Build + Shrinkwrap，不需要额外插件）

- **本机现状**：Blender自带，不需要安装任何东西；`RetopoFlow`（CG Cookie出品的专业重拓扑插件）本机未安装，是收费插件，如果后续想要更顺手的手工重拓扑体验可以考虑购买/安装，但不是必需——vanilla Blender的"Poly Build"工具 + "Shrinkwrap"修改器组合已经是业界常见的手工重拓扑标准做法。
- **命令行/自动化能力**：**低**——这是交互式建模操作，本质上需要人（或者非常复杂的启发式脚本）逐个决定每一条边线怎么画，不适合批量脚本自动化，只能"人工操作、脚本辅助校验"。
- **保持外观**：**最可控**——因为每一步都是在参照高模表面上直接操作（Shrinkwrap实时投射），人工可以随时肉眼确认没有走样。
- **对8个关键区域的改善能力**：**最高，且是唯一能100%保证的**——人工建模时可以直接按照"肩要有完整环""肘要有独立中间环""眼周嘴周要有同心环"这些明确的拓扑设计要求去搭建，不依赖算法能否"猜"到关节在哪。
- **代价**：工作量最大，且如果由AI通过脚本远程操控完成（没有真人美术手动操作鼠标画线），实际上是用大量小粒度的顶点/边操作脚本去模拟人工建模决策，效率和质量都会受限于脚本设计的精细程度，不是真正意义上的"人工建模"，需要如实管理预期。

---

## 二、方案对比总表

| 方案 | 本机现状 | 命令行/自动化 | 方向性Edge Flow能力 | 保外观能力 | 8区域关节改善 | 主要风险 |
|---|---|---|---|---|---|---|
| A. Instant Meshes | 未装，需编译，兼容性不确定 | 支持纯CLI，但"引导曲线"复现需额外工作 | ★★★★（最佳，但关节处仍需引导） | 好 | 中-高（需配合引导曲线） | 构建/兼容性成本 |
| B. Blender QuadriFlow | 已装但本模型上跑不通 | 原生bpy支持 | ★★★★（若能跑通） | 好 | 中-高（同样需要引导） | 已知在本模型上失败，根因未定位 |
| C. MeshLab/pymeshlab | 未装，pip可装 | 最好（纯Python） | ★★（无方向场，类似Tris to Quads） | 一般 | 低 | 达不到目标质量 |
| D. 手工/半手工（Poly Build+Shrinkwrap） | 已装，无需安装 | 差（本质人工操作） | ★★★★★（人工可控） | 最好 | 最高（唯一可100%保证） | 工作量大，脚本模拟人工效率有限 |

---

## 三、推荐方案：自动打底 + 局部半手工重建关键区域 + Shrinkwrap回高模（混合方案）

不追求100%自动化，目标是拓扑质量而非自动化完成率，具体建议：

1. **打底阶段**：优先按 A → B 的顺序尝试自动方向场重网格化——
   - 先评估Instant Meshes的编译/获取成本是否可接受（如果可以在不影响服务器稳定性的前提下编译，它是开源工具里方向场质量最好的选择）；
   - 如果编译成本/兼容性风险评估后不划算，退而尝试**换一个不同的Blender发行版本重新测试QuadriFlow**（成本低，值得一试，但不保证解决）；
   - 两者都不可行时，接受"自动打底只能做到轮廓层面"的现实，直接进入第2步，用当前已保留的 `AHAN_LOD0_VISUAL_DRAFT`（18,924面版本）的外观作为参照基础，但**不采用它的拓扑**。
2. **局部半手工重建阶段（决定质量的关键一步，不可跳过）**：无论第1步用哪个工具打底，眼周、嘴周、颈、肩、肘、腕、功能性髋、膝这8个区域**都需要单独用Poly Build+Shrinkwrap人工重建边线环**——这是本次调研最重要的结论：**因为阿韩是没有尖锐解剖特征的浑圆Q版角色，任何纯自动方向场算法都无法仅凭曲率信息猜中"功能性关节"该放在哪，这8个区域的正确Edge Flow本质上是一个需要人（或高精度脚本模拟人工判断）来做设计决策的问题，不是算法能单独解决的**。
3. **收尾阶段**：全部顶点最终用Shrinkwrap投射回2A产出的干净体素重网格化模型（`AHAN_HIGH_REF_500K`的Voxel Remesh结果，单一连通、0非流形，比原始碎片高模更适合做Shrinkwrap的投射目标）表面，确保外观贴合。

---

## 四、文件与版本状态（本次调研未新增/未修改任何模型文件）

- `AHAN_LOD0_VISUAL_DRAFT`（原 `AHAN_LOD0_DRAFT_v1`，18,924面，Decimate+Tris to Quads产物）：按你的指示，**降级定义为仅供外观校验参考，不作为最终动画拓扑方案**，文件位置不变（`ahan_retopo_lod0_v1.blend`），未删除。
- 2A产出的干净体素重网格化版本（单一连通、0非流形、0自相交，约53,910面）：保留在 `ahan_retopo_work.blend` 中，作为下一步无论选哪个重拓扑工具都可以直接使用的输入。
- 原始500K高模：全程只读，未做任何修改。
- 本次调研新发现的 `rig-v01`、`a-han-head-master` 系列：均为服务器上既有文件，本次调研只做了只读查看，未修改。

---

## 五、待你确认的下一步（本文档仍只是调研，不含任何执行）

1. 是否要投入尝试编译/获取 Instant Meshes？（有一定不确定性和时间成本）
2. 是否要先低成本试一次"换Blender版本重跑QuadriFlow"？
3. 是否直接跳过纯自动方案，接受"打底+8区域人工重建"的混合方案作为主路线？
4. 是否需要我先去看一眼 `rig-v01` 和既有关键点估算的具体数值差异，作为后续重拓扑关节定位的参照基准之一？

以上均未执行，等待你的方向确认。
