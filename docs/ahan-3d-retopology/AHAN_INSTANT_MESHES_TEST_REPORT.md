# 阿韩 Instant Meshes 自动打底实验报告（阶段2B-IM）

- **前置文档**：[AHAN_RETOPOLOGY_PLAN.md](AHAN_RETOPOLOGY_PLAN.md)、[AHAN_RETOPOLOGY_TOOL_INVESTIGATION.md](AHAN_RETOPOLOGY_TOOL_INVESTIGATION.md)、[AHAN_RIG_V01_MOTION_ANALYSIS.md](AHAN_RIG_V01_MOTION_ANALYSIS.md)
- **本阶段范围**：只做自动打底实验与验收判断。**未绑骨、未做权重、未做Shape Key、未烘焙、未制作动画**。原始500K高模、`rig-v01`、2A基准模型（`ahan_retopo_work.blend`）**均未修改**，所有实验产物都是新文件。

---

## 一、安装过程（低成本路径，未长时间排错）

1. 二次确认本机不存在任何形式的Instant Meshes二进制。
2. 检查官方 `wjakob/instant-meshes` 仓库：GitHub Releases为空（官方从未发布预编译二进制），源码编译需要 CMake（本机未装）+ Qt5 + NanoGUI + TBB + Eigen 等重GUI依赖链，且本机**没有免密sudo权限**，无法安装系统级开发包——这是一个硬性阻塞，不是"复杂但可尝试"的情况，按你的指示直接放弃源码编译这条路，没有花时间排错。
3. 转而检查PyPI：找到 `pyinstantmeshes`（1.0.0），是一个用pybind11封装的、**独立重新实现Instant Meshes核心算法的Python库**，提供与本机Python 3.14完全匹配的预编译manylinux wheel（549KB），**不需要编译，不需要sudo**。
4. 在独立的Python虚拟环境（`/tmp/.../im_test/venv`，不影响系统/conda环境）中 `pip install pyinstantmeshes numpy`，安装成功，验证可用。

**结论：安装成本很低，10分钟内完成，未触发"复杂依赖/编译问题"的停止条件。**

---

## 二、实验设置

- **输入**：重新生成了一份干净的2A基准（不复用之前混乱保存历史的`ahan_retopo_work.blend`，避免歧义），流程与此前验证过的完全一致（Voxel Remesh 0.008m×11次迭代 + 局部补丁修复自相交），结果：53,902面、0非流形、0自相交、单一连通，导出为 `ahan_2A_clean_base.obj`（新文件，未覆盖任何已有文件）。
- **参数测试范围**：按"不盲目跑大量参数"的要求，只测试了3个目标密度做初筛（8,000 / 10,000 / 13,000面），发现"`pure_quad=True`"模式会强制细分导致面数膨胀到目标值的4倍（10,000目标变成40,729个四边面），不符合LOD0面数区间要求，改用默认的自然四边面为主输出模式（少量三角/五边"奇异点"是四边面拓扑的正常特征，不是缺陷）。基于初筛结果，最终选定两个候选做完整验收：**IM_10000**（目标10,000面）与**IM_15000**（目标15,000面）。

---

## 三、技术检查结果对比

| 指标 | 原始高模500K | Decimate LOD0_VISUAL_DRAFT | **IM_10000** | **IM_15000** |
|---|---|---|---|---|
| 面数 | 500,000 | 18,924 | 10,220 | 15,385 |
| 四边面占比 | 0% | 58.5% | **96.5%** | **97.0%** |
| 三角面等效数 | 500,000 | ~30,000 | ~20,280 | ~30,550 |
| 非流形边 | 96,950 | 0 | 16 | 4 |
| 非流形顶点 | — | 0 | 6 | 5 |
| 网格岛数量 | 3,501 | 1 | **3** | **1** |
| 自相交三角对 | 大量 | 9 | 0 | 2 |
| 左右对称度(1cm容差) | 99.36% | 未测 | 97.91% | **99.36%** |
| 包围盒 | 0.778×0.639×1.104m | 基本一致 | 基本一致 | 基本一致 |

**IM_15000 在几乎所有硬指标上都优于IM_10000**（单一连通 vs 3个孤岛、对称度99.36% vs 97.91%），且四边面占比和自相交数量都在可接受范围，**确定IM_15000为主推荐候选**。两者的四边面占比（96.5%/97.0%）相比Decimate的58.5%是质的飞跃。

---

## 四、外观检查（正面/背面/左侧，Clay渲染，与原高模、Decimate版对比）

- **整体轮廓**：与原高模、Decimate版本高度一致，矮胖圆润的比例保持良好。
- **三根头发**：清晰可辨，形态保留完整。
- **耳朵**：形态、朝向保留良好，内耳卷曲细节仍可辨认。
- **眼睛/鼻子/嘴**：五官清晰可辨，位置比例正确。
- **拳套手**：三瓣圆润造型保留，与前臂过渡自然。
- **马甲**：领口、下摆、系带、纽扣轮廓保留，仍能看出层次。
- **尾巴**：背面圆形凸起清晰保留。
- **⚠️ 已知瑕疵（非拓扑问题，有已知后续修复路径）**：表面有轻微的分块/噪点感，在开启平滑着色（Shade Smooth）后依然存在，判断是**继承自2A基准本身**——2A为了消除自相交，voxel remesh分辨率定在8mm，这个分辨率本身会给表面带来轻微离散化噪点，Instant Meshes在重投影阶段忠实保留了这个噪点。**这个问题会在方案既定的"最终Shrinkwrap回原始500K高模表面"步骤中被自然修正**（届时投射目标是原始高模的真实光滑表面，而不是8mm体素化的中间表达），不应算作Instant Meshes本身的拓扑缺陷，也不影响本阶段"打底拓扑是否可用"的判断。

---

## 五、Edge Flow 检查（线框渲染，重点区域，坐标已采用rig-v01验证过的功能基准）

用Wireframe修改器叠加真实拓扑线框渲染了肩(Z≈0.60)、肘(Z≈0.46)、腕(Z≈0.34)、髋(Z≈0.28)、膝(Z≈0.18)、颈(Z≈0.70)、眼周、嘴周共8个区域，以IM_15000为对象：

- **全身线框总览**：呈现出清晰、规则、密度均匀的四边面网格，明显沿身体曲面走向铺设，**不是杂乱三角面拼接**——这与阶段2C里Decimate方案的"完全无方向性散乱拓扑"形成鲜明对比。
- **肩、肘、腕、髋、膝、颈、眼周、嘴周**：这8个区域的局部网格都是**规则、近似正方形、无破面的四边面网格**，边线走向在这些区域呈现统一的斜向"网格布"图案。
- **诚实的判断**：这个"网格布"图案是**均匀铺满全身的一个全局方向场结果，不是专门针对关节做的环绕式边线环**——也就是说，Instant Meshes确实做出了干净、规则、密度合理的四边面网格，但它并**没有"聪明地"在肩、肘、腕这些位置自动转出一圈刚好围绕肢体截面的功能性边线环**（这与阶段2C工具调研报告的预判完全一致：阿韩没有尖锐解剖特征，纯自动方向场算法猜不出关节该在哪）。
- **但这不等于"不可用"**：网格本身足够干净、密度足够、完全没有非流形/破面问题，这意味着在这8个区域**局部重新定向一小圈边线（让其中一圈边线严格贴合关节功能高度并围绕肢体截面闭合）是一个局部的、有清晰参照的编辑任务**，而不需要在混乱的三角面废墟上从零开始建模——这正是决策9里"不要求自动生成完美关节拓扑，只判断主体是否干净到只需局部重建"这个问题的核心答案。

---

## 六、验收结论

按你设定的判断标准逐条对照：

- 主体拓扑是否明显优于Decimate：**是**（四边面占比97% vs 58.5%，单一连通vs需要人工评估的碎片化风险，边线呈规则方向性网格vs完全杂乱三角面）
- 外观是否基本保持：**是**（三根头发、耳朵、五官、拳套手、马甲、尾巴均清晰可辨，比例吻合原高模；仅有的表面噪点问题继承自2A基准且有既定后续修复路径，非本阶段需要解决的问题）
- 大面积区域Edge Flow是否可用：**是，可用作局部重建的基础**（规则四边面网格，无非流形/破面，但需要在8个关键区域做局部重新定向以形成真正围绕关节的功能性边线环）

### 最终结论：**A —— Instant Meshes 可作为正式重拓扑基础**

采用方式：以 **IM_15000**（15,385面，97%四边面，单一连通，0关键缺陷）作为下一阶段的主体拓扑打底，在肩(0.60)、肘(0.46)、腕(0.34)、髋(0.28)、膝(0.18)、颈(0.70)、眼周、嘴周这8个区域做局部半手工边线重定向（对应`AHAN_RETOPOLOGY_PLAN.md`第六节方法论里"局部半手工重建关键动画区域"这一步），最后统一Shrinkwrap投射回原始500K高模表面以消除继承自2A基准的表面噪点、恢复真实细节精度。

---

## 七、本阶段产出文件（均为新文件，未覆盖/修改任何既有文件）

- `/tmp/.../im_test/venv/`：隔离的Python虚拟环境（仅本次会话临时使用）
- `/tmp/.../im_test/ahan_2A_clean_base.blend` + `.obj`：重新生成的干净2A基准（新文件）
- `/tmp/.../im_test/im_natural_8000.obj`、`im_natural_10000.obj`、`im_natural_13000.obj`、`im_natural_15000.obj`：Instant Meshes候选结果
- `/tmp/.../im_test/candidates_analysis.json`：技术指标原始数据
- `/tmp/.../im_test/renders/`：全部对比渲染图（Clay + 线框）

原始500K高模、`rig-v01`、此前的2A/2C相关文件全程未被访问写入。

---

## 八、下一步

结合本报告选定的 **IM_15000** 作为打底拓扑，以及 `AHAN_RIG_V01_MOTION_ANALYSIS.md` 里修正后的关节功能坐标和形变范围，可以开始规划8个动画关键区域的具体半手工重建执行方案。本报告仅完成打底方案验收，尚未开始该项工作。
