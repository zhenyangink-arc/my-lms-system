# 阿韩（A-Han）重拓扑实施方案 v2（已按产品决策修订）

- **前置文档**：[AHAN_3D_MODEL_AUDIT.md](AHAN_3D_MODEL_AUDIT.md)
- **版本说明**：本版本已根据产品/美术的 8 条决策修订（原 v1 方案中的"待确认事项"已全部拍板，见下方"决策记录"）。
- **状态**：方案 + 第一阶段（只读分析）已执行；第二阶段（实际重拓扑建模）尚未开始，需按每阶段验收节点走完 Phase 1 复核后再继续。
- **原始文件安全性**：`learning-agent/companions/a-han/v3/models/AHAN_Hunyuan_Master_500K.glb` 全程只读，本方案永久保留它作为唯一的高模真值来源（决策6），任何阶段都不会覆盖/删除它。

---

## 〇、决策记录（本次修订依据）

| # | 决策 | 影响 |
|---|---|---|
| 1 | 脸部走**几何表情路线**；本阶段不做 Shape Key，但新拓扑必须为眨眼/嘴部开合/AI口型预留合理边线 | 3.3 脸部拓扑方案整体改写为路线A |
| 2 | 马甲**不与身体合并**，Body/Vest 保持两个独立、可共同蒙皮的网格 | 3.5、五(UV/材质) 改写 |
| 3 | 三根头发保持**可独立控制**（独立网格或独立拓扑区），不改变外观 | 3.2 改写 |
| 4 | 保留拳套手，不加真人五指；以后靠左右手骨骼 prop socket 挂道具 | 3.4 微调，明确socket方式 |
| 5 | LOD0 面数不强制卡 6,000–15,000，改为约 **20,000–40,000 tris** 参考区间，优先保外观和表情能力；后续追加 LOD1(10K-20K)/LOD2(3K-8K)，均非硬指标 | 四 改写为多级LOD |
| 6 | 原500K高模永久只读保存 | 已是既定原则，本版本再次明确 |
| 7 | 重拓扑优先用可靠的自动/半自动方法，但不能为了"自动完成"牺牲拓扑质量；眼周/嘴周/肩/肘/腕/髋/膝/颈 必须单独检查Edge Flow | 新增 六.方法论 与 七.动画关键区域检查清单 |
| 8 | 每个主要阶段完成后必须：技术检查 + 多角度渲染 + 与原高模对比 + 确认Design Lock无明显变化 | 阶段表全部改写，逐阶段挂验收节点 |

---

## 一、不可变更的外观约束（Design Lock，不变）

1. 整体比例（真实世界包围盒 0.778m×0.639m×1.104m 的比例关系）
2. 三根头发的形状、位置、左右基本对称
3. 脸部五官相对位置比例（大眼睛+高光、短吻鼻头、微笑嘴形、脸颊渐变）
4. 拳套手造型（三瓣圆润凸起，不做真人五指分化）
5. 马甲配色与造型（浅蓝色、金色圆扣、前后片层次、领口/袖口/下摆滚边）
6. 左右基本对称（现状态99.36%顶点镜像匹配水准）

任一阶段如判断"为了拓扑质量必须牺牲以上某条"，必须停下单独提出，不得自行取舍。

---

## 二、总体技术路线（不变）：高模只读参照 → 低模重建 → 贴图烘焙回帖

```
[原始高模 500K tris，v3，永久只读] ──(外观与烘焙参照)──┐
                                                        │
[新建 LOD0 动画母模：Body + Vest + Hair 三个独立网格]  ←──┘
        │
        ├─ UV：Body / Vest 各自独立UV（决策2要求"独立材质"能力，因此新增：两者也各自独立材质/贴图，不再共用现状态的单一材质）
        ├─ 贴图烘焙：从高模烘焙 Normal / BaseColor / ORM 到 Body 和 Vest 两套新贴图
        └─ 产出：AHAN_v4_LOD0.glb（新文件，绝不覆盖 v3）
                ↓（后续任务，本阶段不做）
        AHAN_v4_LOD1.glb（10K-20K） / AHAN_v4_LOD2.glb（3K-8K）
```

**关于决策2的重要澄清**：现状高模其实 Body 和 Vest 共用同一张材质/贴图（审计报告已确认整模型只有1个材质），并不是天然分离的。"不合并"不仅是几何要分成两个网格对象，也意味着重拓扑时要**顺带把 Body 和 Vest 的贴图也烘焙成两套独立贴图**，这样才能真正满足"以后换装和独立材质"的目标，否则光是网格分开、贴图还共用一张，换装时无法真正独立换材质。

---

## 三、分部位拓扑设计方案（按决策修订）

### 3.1 身体主体（头+躯干+四肢，Body网格）

不变：肩/肘/腕/髋/膝/颈各留完整环绕边线循环；关节处适当加密；镜像建模保对称。**新增**：Body网格建模时，在马甲覆盖的躯干区域仍需要按人体自然形态延续拓扑（不能因为外面要盖一层Vest就偷工减料，Body自身必须是一个独立完整、能正常蒙皮变形的躯干，Vest只是叠加在外层的第二张网格，两者独立蒙皮到同一套骨骼上）。

### 3.2 三根头发（决策3：独立可控）

- 三根头发建为**独立于Body头部的网格**（或至少在头部大网格里是可以被工具/后续流程干净切出来的独立拓扑区域，起点做成独立网格更省心、后续也更符合"独立控制"的字面要求）。
- 每根头发根部保留一圈干净边线，作为将来"挂一根简易骨骼做弹动"的预留点（本阶段不装骨骼，只是拓扑上不堵死这条路）。
- 外观不变：形状、朝向、左右基本对称与高模保持一致（用高模三根头发的表面做Shrinkwrap/描摹参照）。

### 3.3 脸部（决策1：几何表情路线，本阶段只做拓扑预留）

- **眼周**：在眼球外部区域建模独立的薄眼皮网格（或至少是可以单独动的一圈拓扑区），围绕眼眶做2-3圈同心边线循环，保证以后眨眼Shape Key/骨骼形变时不出现棱角或塌陷。
- **嘴周**：围绕嘴部开合方向做环形边线（类似人脸拓扑里经典的"嘴部orbicularis环"简化版），为以后口型Shape Key（至少要能支撑张开/闭合/微笑等基础几个viseme）预留形变空间。
- **本阶段边界**：只搭好上述边线循环，**不制作任何Shape Key、不定义具体的表情形态**，外观在静态姿势下必须和高模一致。

### 3.4 拳套手（决策4：不变造型 + prop socket方式）

- 拓扑维持三瓣拳套造型，不新增手指分化。
- 手腕处一圈完整边线循环。
- **prop socket做法说明**（本阶段仅拓扑预留，不实际制作）：以后在手掌中心位置各放一个空物体（Empty）作为道具挂点，父级到对应的手部骨骼上，梳子/书/手机等道具直接挂在这个Empty下跟随手部刚体运动，不需要手部本身有张合形变能力——这与拓扑设计无强耦合，本阶段只需要保证手腕→手掌是连续光滑的自然网格延伸即可，不需要为道具预留额外几何。

### 3.5 马甲（决策2：独立于Body的Vest网格）

- Vest作为独立网格建模，覆盖在Body躯干外层，几何上有厚度或者至少是有正反两面法线的薄壳（避免审计中P1-1提到的接缝/背面剔除风险）。
- 领口、袖口、下摆三处开口边缘需要收边处理（做出干净的边缘循环，而不是像原高模一样是开放的破面边界）。
- 金色圆扣/系带等小装饰可以是Vest网格上的一体化凸起，或独立小物体（视实际重拓扑时哪种方式更干净决定，不强制）。
- Body和Vest两者都蒙皮到同一套骨骼（躯干/肩部相关骨骼），实现"共同蒙皮"（决策2原文要求）。

### 3.6 脚部（不变）

脚踝一圈边线循环，脚掌作为小腿网格自然延伸。

---

## 四、目标面数：LOD 分级（决策5，均为参考区间非硬指标）

| LOD | 面数参考区间 | 用途 | 本阶段是否制作 |
|---|---|---|---|
| LOD0 | 约 20,000–40,000 tris | 动画母模，优先保外观完整度和表情能力，作为后续所有LOD的降面起点 | **是，本轮目标** |
| LOD1 | 约 10,000–20,000 tris | 中距离/性能吃紧场景 | 否，后续任务 |
| LOD2 | 约 3,000–8,000 tris | 远景/极致性能场景 | 否，后续任务 |

原则：先把LOD0做到"外观和表情能力都不打折"，具体最终面数由实际建模+渲染对比效果决定，不预先卡死数字；LOD1/LOD2 由LOD0直接做自动减面（Decimate/Quad remesher降精度）生成并单独做外观对比复核，不是重新走一遍手工重拓扑。

---

## 五、UV与贴图烘焙方案（按决策2更新）

1. **Body、Vest 各自独立UV**，互不共享UV空间。
2. **贴图烘焙拆成两套**：从高模分别为 Body 对应表面和 Vest 对应表面烘焙各自的 Normal / BaseColor / ORM，产出两套独立贴图（不再是审计时发现的"全模型共用1个材质"状态）。这一步是真正兑现决策2"独立材质"能力的关键操作，不能省略。
3. 贴图分辨率：随LOD0面数大幅低于原高模，建议烘焙目标定在 1K–2K（对应审计 P3-1 的后续优化建议），具体看烘焙后细节效果，不够再上调。

---

## 六、方法论：可靠优先的自动/半自动重拓扑流程（决策7）

**原则**：允许用自动化工具打底，但每一步自动化产出都必须经过人工/规则化质量检查，不合格必须返工或转手动，不能"为了自动完成"而让关节区域拿到一坨方向混乱的三角面。

推荐流程（在服务器 Blender 5.2.1 中执行）：

1. **构建临时可制造用参照壳**：对高模做一次 Voxel Remesh（体素重网格化），把3,501个碎片岛屿融合成一个单一、封闭、流形的近似外壳。**这一步的产物只作为下一步自动重拓扑工具的输入，不作为最终交付几何**，原高模本身不受影响（全程在内存中的独立副本上操作）。
2. **自动四边面重拓扑打底**：对上一步得到的流形外壳跑 QuadriFlow（Blender内置的四边面重网格化算法），指定目标面数在LOD0区间内，得到一版四边面为主、大致贴合外观的低模初稿。
3. **关键区域人工/规则化复核与修正**（对应决策7点名的8个区域，见下节清单）：QuadriFlow等自动算法**不保证**关节区域的边线走向适合形变，必须逐一检查这8个区域的Edge Flow，凡是环绕方向不对、边线密度不够、或者干脆没有形成闭合环的，都要手动/半自动修正（如用Blender的"Loop Tools"、"Poly Build"精修局部）。
4. **分件切分为 Body / Vest / Hair 三个对象**：自动重拓扑初稿是一整块，需要按第三节方案手动切割成三个独立网格，并各自补齐切口边缘的干净收边。
5. **对称化**：用镜像修改器把左右不对称的自动重拓扑误差校正掉。
6. **每步都用高模做Shrinkwrap校准**：无论是自动还是手动阶段产出的顶点，最终都要投射贴合回高模表面，确保外观（Design Lock）不跑偏。

如果第2步自动结果质量太差（比如8个关键区域大面积不合格、或者整体面数远超LOD0区间还是长得歪七扭八），要如实中止并升级为更多人工介入的半自动流程，不能为了流程好看硬用一个烂结果往下走。

---

## 七、动画关键区域 Edge Flow 检查清单（决策7新增）

每完成一版重拓扑（无论自动还是手动产出），以下8个区域必须逐一单独检查，缺一不可：

| 区域 | 检查要点 |
|---|---|
| 眼周 | 是否有围绕眼眶的同心边线循环；眼皮区域是否可独立于眼球形变 |
| 嘴周 | 是否有围绕嘴部开合方向的环形边线；边线密度是否够支撑基础口型变化 |
| 肩 | 是否有完整环绕肩关节的边线循环；手臂抬起模拟测试是否有明显塌陷/尖角 |
| 肘 | 是否有独立于肩/腕的中间边线循环；弯曲模拟测试表面是否平滑过渡 |
| 手腕 | 边线循环是否完整闭合；手掌与前臂衔接处是否有多余碎面 |
| 髋 | 大腿根部是否有完整边线循环；腿部抬起/分开模拟测试裆部是否穿模 |
| 膝 | 边线循环是否独立、位置是否落在合理弯折点上 |
| 颈 | 是否有独立边线循环支持点头/转头形变（即使当前马甲遮住大部分颈部，仍需保留） |

检查方式：本阶段用一套**临时的、仅用于测试、不作为最终交付物**的简易骨骼（每个关键区域一根骨骼即可）驱动重拓扑网格做基础弯曲测试，渲染对比弯曲前后表面是否平滑，测试完删除临时骨骼，不影响后续正式绑骨工作。

---

## 八、阶段与验收节点（决策8：每阶段都要技术检查+多角度渲染+对比+Design Lock确认）

| 阶段 | 内容 | 产出 | 验收标准（决策8四件套） |
|---|---|---|---|
| **0. 决策确认** | ✅ 已完成（本文档"决策记录"） | 本文档v2 | — |
| **1. 参照与关键高度分析** | ✅ 本次已执行：只读导入高模，计算真实世界包围盒/半径剖面，定位领口/肩/腋下/腰/裆部/膝/脚踝等关键高度带，对3,501个岛屿做Vest/Body/头发/五官/手脚的初步启发式分类 | 关键区域高度表 + 岛屿分类统计 + 标注参照渲染图 | 只读操作，原文件未变；本阶段不产生新几何，Design Lock天然未受影响 |
| **2. Body主体自动打底+人工复核** | Voxel Remesh → QuadriFlow → 8大关键区域Edge Flow人工复核修正 | Body低模（未切分Vest/Hair前的整体） | 技术检查（连通性/流形性）+ 多角度渲染 + 与高模对比 + Design Lock确认 + 8区域Edge Flow清单逐项过 |
| **3. 切分Body/Vest/Hair三网格** | 按3.2/3.5方案切分并各自收边 | 三个独立网格 | 同上四件套 + 确认三网格可共同蒙皮、边缘无破面 |
| **4. 脸部眼周/嘴周精修** | 按3.3方案加密/调整边线循环 | 更新后的Body头部区域 | 同上四件套，额外确认眼周嘴周边线闭合 |
| **5. UV展开+双材质烘焙** | 按五方案，Body/Vest各出一套贴图 | 两套UV+两套贴图 | 烘焙后渲染与原高模逐视角比对，颜色/细节无明显丢失 |
| **6. LOD0技术终检** | 重跑bmesh技术检查脚本 | 技术检查报告 | 网格岛数量收敛合理、无非预期非流形、面数落在20K-40K参考区间或有理由的偏离说明 |
| **7. 导出LOD0** | 导出`AHAN_v4_LOD0.glb`，本阶段不上传R2（待用户确认后再上传） | 新文件 | 不覆盖v3；本地存档 |
| 8+（后续任务，本次不做） | LOD1/LOD2生成、正式绑骨、Shape Key制作、道具Socket实装 | — | — |

---

## 九、本方案明确不包含的内容（更新）

- 不直接在原500K高模上做焊接/合并/补洞等"暴力修复"操作，高模永久只读（决策6）。
- 本阶段不制作任何Shape Key、不绑定正式骨骼（决策1、7中的"临时测试骨骼"用完即删，不算正式绑骨）。
- 不实装道具Prop Socket，只在拓扑上不堵死这条路（决策4）。
- 不生成LOD1/LOD2（决策5，留待LOD0确认后的后续任务）。
- 不改变阿韩现有角色设计（比例、头发、五官、拳套手、服装造型）——Design Lock六条贯穿所有阶段。

---

*本方案v2基于产品8条决策修订，第一阶段（只读关键高度分析）已在本次会话中执行完毕，详见下方"第一阶段执行结果"。第二阶段（Body主体自动打底）尚未开始，需等待本文档确认后继续。*

---

## 十一、阶段2A/2B/2C执行结果（2025-08-31，本次会话）

**结论先行：2A完成良好；2B未能按原计划用QuadriFlow完成，改用替代方法后外观保留良好但Edge Flow质量不合格；2C验收判定为"不通过，暂停进入2D"，需要先解决拓扑流向问题。原始高模全程只读，未做任何修改。**

### 11.1 阶段2A：Voxel Remesh 融合碎片外壳 —— 通过

在工作副本（复制自只读高模，高模本身未被触碰）上，用 Voxel Remesh（体素尺寸0.008m，迭代11次收敛）把审计报告P0-1提到的3,501个碎片网格岛，成功融合成了**1个完全连通、0非流形边、0非流形顶点的封闭壳体**。这一步达成了预期目标。

副作用：融合过程中在"手贴腹部""脚趾之间""头发根部与头皮交界"这几处（原本就是分开建模、紧贴在一起的部位）产生了残留的几何自相交（最终收敛在15-20对三角面级别，占比极低），已通过局部"挖除+补洞"的方式修复到0。

### 11.2 阶段2B：四边面自动打底 —— QuadriFlow 未能跑通，已改用替代方法

**原计划**使用Blender内置QuadriFlow算子生成四边面拓扑，但遇到了持续性阻塞：QuadriFlow反复报错"网格必须是流形，并且具有指向一致方向的面法线"并直接取消执行，即使网格已确认满足以下全部条件（逐一排查记录）：

- 非流形边：0；非流形顶点：0（含"蝶形顶点"检测）
- 边界边：0；游离边：0；零面积面：0
- 自相交三角对：0（用BVH自碰撞检测复核）
- N-gon：0（全部三角化）
- 欧拉示性数 V-E+F=2，确认亏格为0（拓扑上等价于球面，非病态拓扑）
- 排除对称选项、目标面数、三角/四边混合拓扑等参数影响（逐项单独测试均无关）
- 用全新导出再导入的OBJ文件（排除任何残留缓存/自定义数据层干扰）重新测试，结果相同
- 用默认UV球、细分立方体等简单模型验证QuadriFlow算子本身在此Blender版本（5.2.1 LTS）中功能正常

排查已覆盖所有可通过Python脚本验证的流形/拓扑/参数维度，结论是：**这是当前服务器Blender构建的QuadriFlow实现，在处理这类含耳朵内卷、指缝、发丝根部等高曲率细节的复杂有机形状时存在一个未能定位根因的内部限制/疑似缺陷**，不是本方案流程或网格数据存在可自行修复的错误。

**临时替代方案**：改用 Decimate（比例衰减）+ Tris to Quads 完成2B的"面数收敛"目标，产出了一版18,924面（7,848三角+11,076四边，四边面占比58.5%，三角面等效约30,000面）、0非流形、单一连通壳体的LOD0草稿（`AHAN_LOD0_DRAFT_v1`）。

### 11.3 阶段2C验收结果

**技术检查**：

| 指标 | 原始高模500K | LOD0草稿v1 |
|---|---|---|
| 顶点数 | 300,206 | 15,002 |
| 面数（三角/四边） | 500,000 / 0 | 7,848 / 11,076 |
| 三角面等效数 | 500,000 | ~30,000 |
| 四边面占比 | 0% | 58.5% |
| 非流形边 | 96,950 | 0 |
| 网格岛数量 | 3,501 | 1 |
| 自相交三角对 | 未测（原模型本身大量碎片重叠） | 9（Decimate引入的极少量残留，可忽略） |
| 世界包围盒 | 0.778×0.639×1.104m | 0.778×0.639×1.104m（几乎完全一致） |

**多角度渲染对比**（新旧网格用同一相机参数、同一素模灰材质渲染对比，共24张）：正面、背面、左侧、右侧、左前45°、右前45°在正常观看距离下**外观高度一致**，Design Lock六条约束（整体比例、三根头发、五官、拳套手、马甲、左右对称）在这些常规视角下均未见明显劣化或走样。

**极限特写检查**（耳朵内部、马甲滚边、头发根部等，远超正常使用场景的放大倍数）：能看到decimate产生的多边形棱角化痕迹（不平滑），头发根部特写里甚至出现了一处可疑的三角形缺口，需要在后续步骤里确认是渲染阴影折痕还是真实几何缝隙——这一层级的瑕疵在正常展示尺寸下不会被注意到，但如果后续要做贴图法线烘焙或超近景特写镜头，需要重新核实。

**⚠️ Edge Flow检查（决策7要求的核心项）—— 不合格**：用Wireframe修改器叠加渲染出真实拓扑线框后发现，肩、肘、颈、眼周等全部关键区域，边线走向都是Decimate算法基于误差度量随机决定的散乱三角面拼接，**完全没有围绕关节/五官的方向性边线环**。这不是"需要微调"的程度，而是结构性缺失——Decimate+Tris to Quads这个替代方法从原理上就不具备生成有方向性边线流的能力，继续在此基础上做2D的"精修"实际上等于要从零手工重建全部拓扑，不是"优化"而是"重做"。

### 11.4 判定与建议

**判定：2C验收不通过，暂停进入2D。** 原因：外观保留达标，但Edge Flow这项决策7明确要求"必须单独检查"且"不能为了自动完成牺牲拓扑质量"的核心指标未达标，且当前替代方法无法通过参数调整解决（Decimate从原理上不具备方向性拓扑能力）。

**三个可选的后续方向**（需要产品/你来决定选哪个，我不替你做这个决定）：

1. **继续排查/等待QuadriFlow**：这条路已经投入较多排查仍未解决，继续下去大概率还是走不通，除非有人能确认这个具体Blender构建的已知问题或找到新版本。
2. **手工/半手工重拓扑（RetopoFlow/PolyBuild + Shrinkwrap）**：以当前的LOD0草稿（外观已验证良好）或原高模为参照，人工在关键区域搭建真正的边线环，工作量显著高于原计划的"自动打底+局部精修"，但是能保证Edge Flow质量，是当前唯一确定可行的路径。
3. **寻找/引入其他自动四边面重网格化工具**（如Instant Meshes，本机未安装，需要联网获取）：可能比QuadriFlow更稳定，但引入新工具本身也有一定不确定性，需要先验证。

以上均未执行，等待你确认方向后再继续。

---

## 十、第一阶段执行结果（只读分析，已完成）

方式：在Blender中重新只读导入高模，对全部300,206个真实顶点做了两类数据驱动分析：①沿Z轴（世界坐标真实高度，已规避审计报告P3-3提到的`obj.dimensions`陷阱）的"到中轴半径"剖面，寻找轮廓局部极值；②在下半身逐高度切片检测双腿是否存在X方向的分离间隙，定位髋部分叉高度；③单侧手臂局部剖面定位手腕最细处。原文件未做任何修改。

### 10.1 关键高度带（相对总高1.1038m的比例，供Phase 2定位边线循环参考）

| 高度比例 | 绝对高度(m) | 观测到的轮廓特征 | 初步解读（供人工在Phase 2用高模复核，非最终结论） |
|---|---|---|---|
| 0% | 0.000 | 脚底/地面 | — |
| 32.7% | 0.361 | 半径局部**最大值**（0.390m，是全身所有极值点里半径最大的一个） | 大概率是圆滚滚的腰腹最宽处 |
| 58.6% | 0.647 | 半径局部最小值（0.283m）；与双臂手腕最细处的估计高度（58.3%）几乎重合 | 疑似马甲下摆上方的"腰身收窄"，双手自然下垂时手腕也大致在这个高度 |
| 69.5% | 0.767 | 半径局部最大值（0.332m）；与独立算法估算的双眼高度（69.3%）吻合 | 脸颊/眼睛所在的头部最宽处之一 |
| 82.3% | 0.908 | 半径局部最小值（0.298m） | 头部轮廓内部的一次收窄（额头到耳朵之间），**不是传统意义的颈部**——见下方重要发现 |
| 90.0% | 0.993 | 半径局部最大值（0.325m） | 耳朵向外撑开的最宽点 |
| 100% | 1.104 | 头顶 | — |

### 10.2 两个重要且非预期的发现

1. **没有传统的髋部/大腿分叉结构**：从下往上逐高度扫描双腿间是否存在X方向间隙，结果只在极低处（约5.5%高度，即离地约6cm，基本就是两只脚本身分开的位置）检测到间隙，再往上直到腰腹都是一个连续浑圆的整体，**不存在大腿分开的"裆部"几何**。这与视觉印象一致（阿韩是矮墩墩的不倒翁式身形，脚是直接从浑圆身体底部长出来的两个小凸起，不是有大腿的人形腿）。**影响**：审计和v1方案里提到的"髋/膝"边线循环，在这个角色身上不对应任何现成的几何转折点，Phase 2需要在原方案默认的"合理弯折位置"上做功能性放置（为了以后腿部动画留形变空间），而不是照着现成的解剖学凹陷去描线，这类边线的具体位置需要人工在高模上目测确定，本阶段的数据只能证伪"有明显裆部凹陷"这件事，给不出更精确的替代位置。
2. **头部到躯干之间没有测到明显的颈部收窄**：58.6%到82.3%之间半径先降后升，但82.3%处的收窄经交叉核对更像是头部内部（额头到耳朵之间）的轮廓变化，而不是独立的颈部；结合马甲领口挡住了大部分颈部区域，阿韩很可能是"头直接接身体、颈部极短或视觉上不存在"的Q版比例。**影响**：3.1节要求的颈部独立边线循环，在Phase 2里同样需要功能性放置（大致定在马甲领口贴合下巴的那条线附近），而不是依据一个实测到的几何凹陷。

### 10.3 五官近似定位（供Phase 2参照，非最终坐标）

- 双眼平均位置（近似）：左眼 (0.044, -0.245, 0.765)，右眼 (-0.040, -0.246, 0.764)——左右对称、高度一致，交叉验证与10.1的69.5%高度极值吻合。
- 鼻/嘴中心区域平均位置（近似）：(0.001, -0.262, 0.764)。
- 以上坐标是"头部最靠前8%顶点按左右中心分组求均值"的粗略聚类结果，实际眼周/嘴周边线循环的精确走线仍需Phase 2在高模表面上用Shrinkwrap对照人工确定，这里只提供一个可信的初始定位，避免从零开始盲找。

### 10.4 一张辅助可视化图（已知有渲染瑕疵，仅供粗略参考）

已生成 `13_phase1_height_markers.png`，用半透明色带标出上表7个高度。**已知问题**：Eevee对多层半透明面片的排序和自发光效果叠加在一起产生了颜色晕开/条带模糊的效果，色带边界不如预期清晰，头顶上方还出现了辉光溢出到背景的现象。这张图只作为"大致方位感"的辅助参考，**精确数值以10.1表格为准**，不建议在此图上直接量取坐标。

### 10.5 结论

第一阶段未发现任何需要回头修改Design Lock或本方案其他章节的信息；两个"非预期发现"已经作为一次性说明整合进3.1节的隐含前提（髋/膝/颈边线循环是"功能性放置"而非"贴合解剖凹陷"），不需要因此改变已批准的方案结构。**建议直接按方案进入阶段2（Body主体自动打底：Voxel Remesh → QuadriFlow → 8大关键区域人工复核）**，静待确认后再执行。
