# 教材制作工作区改造

2026-09-13。用户不满意原教材管理页的表格堆叠，本轮改为章节选择与内容制作工作区。

## 实际改动

- 左侧章节目录，支持名称搜索，教材分组，当前章节高亮；课程导览不算作教学章。
- 右侧当前章节工作区，包含内容制作、检查与发布、教材设置。移除整页大表格和重复指标卡片。
- 默认选真实第一章（有匹配ID时），否则首个可用章节。显式未知章节不静默替换。切换章节写入现有chapter查询参数，刷新恢复选择。
- 只在教材制作页面收起旧全局章节定位表单；课程结构、脚本等页面继续原导航。上方四个工作流导航保留。
- 通过原查询增加 `digital_textbook_chapters.title`，复用 localizedTitle，读取真实中文章节名称；无标题时仅回退章节序号，不猜译slug。数据库列已有，无migration。
- 内容区显示原词汇和语法编辑器可读条目的预览，复用原编辑弹窗。专用编辑器语法条目为零不表示Runtime语法内容为零，提示中明确这一口径。
- 第一章保留真实工作台链接，其他章节不伪造支持。发布操作只在当前章“检查与发布”中显示，继续原确认和Action；教材整体状态放“教材设置”，影响范围明确。
- API写入、鉴权、发布、Runtime、学生数据保持原实现。读取不完整时禁用本页制作操作，显示刷新提示。

## 隔离验收

- 27/27相关测试通过：Chromium教材工作区、课程工作流、章节上下文、管理路由。
- 浏览器渲染真实Listing、CourseWorkflowNavigation和Studio，17条合成目录数据模拟课程导览加16章。无生产数据库或负责人身份冒用。
- 验证目录选择、搜索空结果、第一章链接范围、URL与刷新恢复、未知章节、只读权限、发布确认取消、设置与编辑入口分离。
- 编辑弹窗在本UI测试中使用参数探针，验证词汇/语法参数；未声称做了实际编辑保存E2E。真实编辑器实现未修改。
- TypeScript（next typegen后）和修改应用文件ESLint通过。部署基线git apply --check --whitespace=error通过。
- 1440/375无横向溢出。截图包含真实工作流导航和后台主题容器，未模拟已登录生产负责人完整外壳。浅色/深色截图保存在docs/evidence/textbook-studio-20260913。
- 未重复全套SQL/Runtime回归。生产构建与部署尚未执行。

## 交付

隔离源码：`/tmp/uply-textbook-studio.2SgjEc`。

增量补丁：`docs/SMART_TEXTBOOK_STUDIO_REDESIGN.patch`，8个文件，SHA256 `fa05175c01700ddacef3e1efbc12aa7d8ace9b82bfe19b67b59663e53981298e`。

基线：`/home/yangzhen/releases/uply-first-enable-20260910/source`，已部署Build ID `EzBxyAk2YmN4y_wRDAPch`，包含前轮MANAGEMENT_ORGANIZATION补丁。此补丁不能同前轮重复叠加。主开发目录未提交修改保留，没有业务回写。

本轮使用UI/UX技能的层级、逐步展开、响应式与键盘可达原则；复用CardTitleWithHint和现有主题。无数据库数据修改、无教材重新发布、无服务重启。上线前仍需正式构建与健康检查。

## 用户授权部署结果

用户回复“部署”后完成应用更新。以上“尚未部署”描述为实施交付时状态，此节记录最终结果。

- 补丁SHA及8个文件基线校验通过，构建与切换间源码摘要、受限运行配置摘要未变化。
- 独立候选目录：`/home/yangzhen/releases/uply-first-enable-20260910/textbook-studio-2026-09-12T15-26-16-591Z`。目录700，清单与私有构建日志600，保存原源码文件和previous-next作为恢复产物。
- 使用候选外受限真实配置直接运行 `next build --webpack`，编译、TypeScript和构建成功。未运行npm prebuild或让测试进程获得真实配置。
- 新Build ID：`3t0mQTrqIxPszwH_29oxA`。构建完成后，仅停止/重启uply-first-enable切换产物；其他服务PID/status未变化。
- 教材页、工作台8443响应均为200，4项静态资源响应200，部署文件摘要匹配候选。未更改代理、配置、密钥、gate、开机列表。
- 无数据库迁移、教材数据写入或快照发布；主开发目录业务代码未回写。负责人正常登录后的页面验收由用户完成，HTTP健康不冒充负责人E2E。
