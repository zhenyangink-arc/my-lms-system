import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Clock3, MessageCircleQuestion, UserRound } from "lucide-react";

import { HelpTicketReplyForm } from "@/app/dashboard/help/HelpTicketReplyForm";
import { HELP_TICKET_CATEGORY_LABELS, HELP_TICKET_STATUS_LABELS, helpDateFormatter, type HelpTicketCategory, type HelpTicketPriority, type HelpTicketStatus } from "@/app/dashboard/help/config";
import { requireHelpCenterManager } from "@/lib/help-center";
import { HelpTicketManager } from "../../HelpTicketManager";

type Ticket = { id: string; user_id: string; subject: string; description: string; category: HelpTicketCategory; priority: HelpTicketPriority; status: HelpTicketStatus; resolution: string; created_at: string };
type Message = { id: string; sender_kind: "student" | "staff"; body: string; created_at: string };
type Profile = { full_name: string | null; email: string | null };

export default async function AdminHelpTicketPage({ params }: { params: Promise<{ ticketId: string }> }) {
  const { ticketId } = await params;
  const { supabase } = await requireHelpCenterManager();
  const [ticketResult, messagesResult] = await Promise.all([supabase.from("help_tickets").select("id,user_id,subject,description,category,priority,status,resolution,created_at").eq("id", ticketId).maybeSingle(), supabase.from("help_ticket_messages").select("id,sender_kind,body,created_at").eq("ticket_id", ticketId).order("created_at", { ascending: true })]);
  if (ticketResult.error || !ticketResult.data) notFound();
  const ticket = ticketResult.data as Ticket;
  const messages = (messagesResult.data ?? []) as Message[];
  const { data: profileData } = await supabase.from("profiles").select("full_name,email").eq("id", ticket.user_id).maybeSingle();
  const student = profileData as Profile | null;
  return <div className="mx-auto w-full max-w-[1500px] space-y-5 px-4 py-6 sm:px-6 lg:px-8"><Link href="/dashboard/admin/help" className="inline-flex items-center gap-2 text-xs font-black app-muted-text"><ArrowLeft size={14} />返回帮助中心管理</Link><section className="app-card rounded-3xl border p-5 sm:p-6"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full px-2.5 py-1 text-xs font-black" style={{ color: "var(--app-secondary)", backgroundColor: "var(--app-secondary-soft)" }}>{HELP_TICKET_CATEGORY_LABELS[ticket.category]}</span><span className="rounded-full px-2.5 py-1 text-xs font-black" style={{ color: "var(--app-warm)", backgroundColor: "var(--app-warm-soft)" }}>{HELP_TICKET_STATUS_LABELS[ticket.status]}</span>{ticket.priority === "urgent" && <span className="text-xs font-black" style={{ color: "#c94f45" }}>紧急</span>}</div><h1 className="mt-3 text-2xl font-black">{ticket.subject}</h1><div className="app-muted-text mt-3 flex flex-wrap gap-4 text-xs"><span className="inline-flex items-center gap-1"><UserRound size={12} />{student?.full_name?.trim() || student?.email || "学生"}</span><span className="inline-flex items-center gap-1"><Clock3 size={12} />{helpDateFormatter.format(new Date(ticket.created_at))}</span></div><p className="app-muted-text mt-5 whitespace-pre-wrap border-t pt-5 text-sm leading-6" style={{ borderColor: "var(--app-border-soft)" }}>{ticket.description}</p></section><div className="grid items-start gap-5 xl:grid-cols-[minmax(0,1.15fr)_minmax(340px,0.65fr)]"><section className="app-card rounded-3xl border p-4 sm:p-5"><h2 className="flex items-center gap-2 text-lg font-black"><MessageCircleQuestion size={19} style={{ color: "var(--app-accent)" }} />沟通记录</h2><div className="mt-5 space-y-3">{messages.map((message) => <div key={message.id} className={`flex ${message.sender_kind === "staff" ? "justify-end" : "justify-start"}`}><div className="max-w-[85%] rounded-2xl px-4 py-3" style={{ backgroundColor: message.sender_kind === "staff" ? "var(--app-accent-soft)" : "var(--app-soft-bg)", border: "1px solid var(--app-border-soft)" }}><p className="text-xs font-black" style={{ color: message.sender_kind === "staff" ? "var(--app-accent)" : "var(--app-success)" }}>{message.sender_kind === "staff" ? "帮助中心" : "学生"}</p><p className="mt-1 whitespace-pre-wrap text-sm leading-6">{message.body}</p><p className="app-muted-text mt-2 text-[10px]">{helpDateFormatter.format(new Date(message.created_at))}</p></div></div>)}{messages.length === 0 && <p className="app-muted-text rounded-xl border border-dashed p-5 text-center text-xs">还没有追加消息。</p>}</div><div className="mt-6 border-t pt-5" style={{ borderColor: "var(--app-border-soft)" }}><HelpTicketReplyForm ticketId={ticket.id} /></div></section><section className="app-card rounded-3xl border p-4 sm:p-5"><h2 className="text-lg font-black">处理设置</h2><p className="app-muted-text mt-1 text-xs leading-5">状态与处理结果会同步显示在学生端。</p><div className="mt-5"><HelpTicketManager ticketId={ticket.id} status={ticket.status} priority={ticket.priority} resolution={ticket.resolution} /></div></section></div></div>;
}
