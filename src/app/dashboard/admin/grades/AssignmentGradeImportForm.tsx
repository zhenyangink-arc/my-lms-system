"use client";
import { useActionState } from "react";
import { RefreshCw } from "lucide-react";
import { initialGradeCenterActionState } from "@/app/dashboard/grades/action-state";
import { importAssignmentGradesAction } from "@/app/dashboard/grades/actions";
export function AssignmentGradeImportForm({ assignments }: { assignments: Array<{ id: string; title: string }> }) { const [state, formAction, pending] = useActionState(importAssignmentGradesAction, initialGradeCenterActionState); return <section className="app-card rounded-[28px] border p-5 sm:p-7"><h2 className="text-lg font-black">同步作业考试成绩</h2><p className="app-muted-text mt-1 text-xs leading-6">读取该任务最新一次已批改成绩，生成或更新成绩项目。</p><form action={formAction} className="mt-4 space-y-3"><select name="assignment_id" required defaultValue="" className="app-input w-full rounded-xl border px-3 py-3 text-xs"><option value="" disabled>选择作业、测验或考试</option>{assignments.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select>{state.message && <p className="text-xs font-bold" style={{ color: state.status === "error" ? "#c94f45" : "var(--app-success)" }}>{state.message}</p>}<button type="submit" disabled={pending || assignments.length === 0} className="inline-flex items-center gap-2 rounded-xl px-4 py-2.5 text-xs font-black text-white disabled:opacity-50" style={{ backgroundColor: "var(--app-success)" }}><RefreshCw size={13} />{pending ? "正在同步…" : "同步最新成绩"}</button></form></section>; }
