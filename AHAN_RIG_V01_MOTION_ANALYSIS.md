# 阿韩 rig-v01 动画反向分析报告

- **调查对象**：`/home/yangzhen/Edints/blender/a-han/rig-v01/AHAN_Rig_v01.blend`（只读打开分析，未做任何修改，未保存）
- **前置文档**：[AHAN_3D_MODEL_AUDIT.md](AHAN_3D_MODEL_AUDIT.md)、[AHAN_RETOPOLOGY_PLAN.md](AHAN_RETOPOLOGY_PLAN.md)、[AHAN_RETOPOLOGY_TOOL_INVESTIGATION.md](AHAN_RETOPOLOGY_TOOL_INVESTIGATION.md)
- **目的**：**不是复用rig-v01的旧蒙皮**，而是用它已经建好的骨架结构 + 21个已验证动作的真实关键帧数据，反推新拓扑真正需要覆盖的关节位置和形变范围——把"猜测应该在哪加边线环"变成"用已有动画数据算出来应该在哪加"。
- **方法**：用Python脚本读取 `.blend` 文件的骨骼静止姿态坐标（世界空间）+ 全部21个动作的每根骨骼旋转/位移关键帧数值，逐一统计每个关节在所有动作中出现过的最大旋转范围。

---

## 一、rig-v01 骨架总览（41根骨骼，25根参与蒙皮变形）

结构：`CTRL_ROOT → ROOT → CTRL_BODY → pelvis → spine_01 → spine_02 → neck → head`（头部另有独立控制骨`CTRL_HEAD`），四肢为标准 `shoulder → upper_arm → forearm → hand` 与 `thigh → shin → foot` 链，另有 `ear.L/R`、`hair_01/02/03`、`tail` 挂在head/pelvis下。手臂腿部各自建了IK链（`IK_HAND`、`Pole_Arm`、`IK_FOOT`、`Pole_Leg`）与FK共存，`IK_FK`属性切换，默认FK。

**重要架构发现**：头颈的实际驱动方式是"`CTRL_HEAD`约束驱动`head`骨骼整体旋转"，`neck`骨骼本身在全部21个动作里旋转量均为0——也就是说当前设计里颈部不是"颈先弯、头再弯"的两段式，而是"以head骨骼头端(即颈根部)为单一支点，头颈作为一个整体转动"。这对新拓扑是个重要简化：**颈部只需要在这一个支点位置做好一圈边线环，不需要在4cm长的neck骨骼范围内额外分层**。

同样，`pelvis`、`spine_01`、`spine_02` 在全部动作里都没有独立旋转（唯一例外是`AHAN_Idle`里`spine_02`有±2°的呼吸微摆），鞠躬动作`AHAN_Bow`的躯干弯曲是靠旋转`CTRL_BODY`（连带pelvis+spine+head作为一个刚体转12°）实现的，不是逐节脊柱弯曲。**这意味着现阶段动画需求下，躯干可以按基本刚体处理，不需要在腰部/脊柱做额外的多段边线环**（这与我们审计阶段发现的"没有明显腰身收窄"的几何结论也吻合）。

---

## 二、逐关节动作数据反推（核心结论）

### 2.1 肩（shoulder → upper_arm关节）

- **实际关节位置**（静止姿态，世界坐标）：`(±0.25, 0, 0.60)`，即高度比例 **54%**（`shoulder.L/R`骨骼本身是从躯干中心到这个点的一段刚性定位骨，不参与旋转，真正的肩关节转动发生在`upper_arm`骨骼的根部）。
- **实测最大转动范围**：跨21个动作统计，最大单轴转幅出现在 `AHAN_Combing_PreTest`（60°）和 `AHAN_HandsUp`（58°），其余挥手/抬手/开心动作在46-52°区间，走路摆臂只有8-16°。**建议以60°作为形变覆盖上限设计边线环密度**。
- **与此前方案的差异**：此前 `AHAN_RETOPOLOGY_PLAN.md` 里线框检查脚本用的功能性肩部高度是 `0.70*height=0.77m`，比rig-v01实测的0.60m（54%）高出约17cm——**应改用0.60m/54%这个经过动画验证的坐标**。
- **推荐Edge Loop**：以(±0.25,0,0.60)为中心，围绕upper_arm根部做一圈完整环绕手臂截面的边线，并在肩窝与躯干交界处保留足够宽的过渡带（覆盖60°抬升不塌陷）。

### 2.2 肘（upper_arm → forearm关节）

- **实际关节位置**：`(±0.305,-0.005,0.46)`，高度比例 **42%**。
- **实测最大转动范围**：`AHAN_Combing_PreTest`达48°（是全部动作里肘部弯曲最大的一次，符合"梳头"这个动作本身需要大幅屈肘），`AHAN_Thinking`38°，`AHAN_Wave`28°，其余多在18-22°。
- **推荐Edge Loop范围**：肘部需要独立于肩、腕的中间边线环，建议在(±0.305,-0.005,0.46)前后各留一圈过渡边线（三圈结构：肘上、肘、肘下），覆盖48°弯曲时前臂内侧不产生尖锐折角。

### 2.3 手腕（forearm → hand关节，拳套手与前臂的旋转点）—— ⚠️ 此前估算有误，已修正

- **实际关节位置**：`(±0.335,-0.03,0.34)`，高度比例仅 **31%**。
- **⚠️ 重要修正**：`AHAN_RETOPOLOGY_PLAN.md` 第一阶段用几何半径剖面的局部最小值猜测手腕在58.3%高度，这次用rig-v01的真实动作数据核实后发现**猜测严重偏高**——真正的手腕功能位置应该是31%（约0.34m），比之前的估算低了近27个百分点。回看审计阶段最早算出来的"左手聚簇质心"z=0.389（35%），其实比后来那个"58%"的猜测更接近真值。**新拓扑的手腕边线环必须放在31%高度附近，不要用第一阶段方案文档里58%的位置**。
- **实测最大转动范围**：`AHAN_Combing_PreTest`43°（梳头动作对手腕转动要求最高，因为需要手掌翻转贴合头部）、`AHAN_Wave`36°（含Y/Z双轴，即摆手+翻转复合动作）、`AHAN_Thinking`18°、`AHAN_Point`8°。
- **推荐Edge Loop**：手腕处需要完整边线环（对应决策4"拳套手与前臂真正应该发生旋转的位置"），位置定在(±0.335,-0.03,0.34)，不需要手掌内部再分环（因为拳套手不做张合，只需整体跟随手腕旋转）。

### 2.4 髋（功能性髋关节，非解剖学结构）

- 审计阶段已确认阿韩没有真实的大腿分叉/裆部几何。rig-v01在这个前提下，把"功能性髋关节"选定在 `(±0.11,0.02,0.28)`，高度比例 **25%**——这个选择本身就是"没有裆部几何、但仍需要给腿一个转动支点"时的合理折衷位置（浑圆躯干底部与短腿的过渡区）。
- **实测最大转动范围**：`AHAN_Walk_Test`30°（含-16°~14°的前后摆动）、`AHAN_Sit_Test`25°、`AHAN_Step`18°。
- **与此前方案的对比**：`AHAN_RETOPOLOGY_PLAN.md`第一阶段自己猜的功能性髋部高度是28%（0.28*height=0.309m），与rig-v01实际选用的25%（0.28m绝对值）**高度吻合**（差距仅3cm）——这是本次交叉核对里少数"此前猜测就已经蒙对了"的位置，可以直接沿用。
- **推荐Edge Loop**：在(±0.11,0.02,0.28)附近做一圈功能性边线环，明确这不对应任何真实解剖凹陷，纯粹是为了给腿部转动留形变空间，走线时应该顺着浑圆躯干本身的曲面自然过渡，不要试图硬做出"胯部凹陷"的视觉效果（那样反而会破坏Design Lock的"整体比例"）。

### 2.5 膝（thigh → shin关节）

- **实际关节位置**：`(±0.145,0,0.18)`，高度比例 **16%**。
- **实测最大转动范围**：`AHAN_Sit_Test`38°（坐下动作对膝盖弯曲要求最大）、`AHAN_Step`25°、`AHAN_Walk_Test`22°。
- **与此前方案对比**：第一阶段猜的功能性膝部高度是14%（0.155m），与rig-v01实际值16%（0.18m）**基本一致**（差距约2.5cm），同样属于"猜对了"的情况。
- **推荐Edge Loop**：在(±0.145,0,0.18)做独立边线环，覆盖坐下动作38°弯曲的需求。

### 2.6 颈部（头颈整体旋转支点）

- **实际功能支点**：`head`骨骼头端 = `(0, 0.01, 0.70)`，高度比例 **63%**（不是neck骨骼中点，前面已说明neck骨骼本身不参与旋转）。
- **实测最大转动范围**：`AHAN_ShakeHead`左右各14°（合计28°）、`AHAN_Head_Left/Right`各18°（合计36°，是所有头部动作里角度最大的）、`AHAN_Nod`俯仰-8°~12°（合计20°）、`AHAN_Bow`头部仅-5°（鞠躬主要靠身体整体前倾，头部动作很小）。
- **⚠️ 重要修正**：此前线框检查脚本临时用的颈部功能位置是86%高度（几乎在头顶下方一点点），与真实值63%相差极大——63%这个高度实际上就在马甲领口贴合下巴的那条线附近，与第一阶段文档里"颈部功能位置大致定在马甲领口"的**文字描述**是对的，但脚本里写的具体数值（86%）没有对上，此次以rig-v01实测的63%为准。
- **推荐Edge Loop**：在(0,0.01,0.70)附近做一圈完整颈部边线环，覆盖最大36°转动（含俯仰、左右转两个方向的复合需求）。

### 2.7 耳朵、三根头发、尾巴——现有骨骼位置是否值得沿用

| 部位 | rig-v01骨骼位置 | 是否在21个动作中被驱动 | 建议 |
|---|---|---|---|
| 耳朵 `ear.L/R` | 头部(±0.24,0.005,0.88)→(±0.315,0.005,0.965)，高度80-87% | **否**，全部动作中旋转量为0，纯粹跟随head父级 | 位置与审计阶段独立测得的耳朵朝向基本吻合，**值得沿用作为参考位置**，但由于从未被独立驱动过，无法从现有动作反推出"耳朵摆动"所需的形变范围——如果未来不做耳朵二级摆动动画，新拓扑不需要为耳朵单独设计边线环，保持整体一体化即可 |
| 三根头发 `hair_01/02/03` | 头顶偏前(约x=0,±0.075, y=-0.075, z=0.965-0.975)，高度87-88% | **否**，同上，全程跟随head，未独立驱动 | 与审计阶段"头发根部约在Y=-0.17,Z=1.02"的定位方向一致（绝对数值有差异，但都指向头顶偏前区域），**位置可沿用**；同理，若未来要做"梳头时头发被拨动"的二级动画，需要新建根部边线环+可能的简单骨骼链，现有骨骼只是占位，没有实际验证过的形变数据可参考 |
| 尾巴 `tail` | (0,0.17,0.29)→(0,0.325,0.29)，指向+Y（后方），高度26% | **否**，同上 | 位置与背面渲染图里看到的圆形凸起吻合，说明这确实是有意设计的"尾巴/屁股上的圆疙瘩"，而不是审计报告猜测的"徽章"——**建议沿用位置**，但同样没有被验证过的动画数据 |

---

## 三、rig-v01 设计的取舍建议

### 应该继承

1. **骨骼命名与层级结构**：`shoulder→upper_arm→forearm→hand`、`thigh→shin→foot`、`pelvis→spine_01→spine_02→neck→head`这套命名和父子关系清晰、符合行业惯例，新拓扑绑骨时可以直接复用这套骨架设计，不需要重新设计骨骼层级。
2. **关节的世界坐标位置**（肩0.60、肘0.46、腕0.34、髋0.28、膝0.18、颈0.70，均为高度比例下的绝对Z值，已在上文逐一列出）——这些坐标经过实际动作验证（21个动作跑下来没有出现IK Stretch违规、包围盒比例保持在1.4065以内），比单纯几何推测可靠。
3. **IK/FK双系统 + Pole Target设计**：手臂腿部的IK/FK切换、极向量控制骨设计合理，没有发现需要修改的问题，新绑骨阶段可以直接沿用这套控制骨架构。
4. **21个动作的关键帧姿态数值**：这些角度数据本身是有价值的资产（哪怕旧网格被弃用），新拓扑绑骨完成后可以直接把这些姿态数值重新应用到新骨架上做验收测试（见第四节）。
5. **"头颈单支点整体旋转"和"躯干基本刚体"的简化设计**：这不是缺陷，而是合理的工程简化，新拓扑不需要为此过度设计多余的边线环。

### 应该废弃

1. **世界坐标连续包络的自定义权重方案**：这是为了绕开"3,501个断开网格岛无法做骨骼热力学自动权重"这个问题而设计的权宜之计，一旦新拓扑是干净的单一连通网格，应该直接用标准的骨骼热力学自动权重（Bone Heat）或手工权重刷，不需要沿用这套补丁方案。
2. **旧的500K碎片网格本身**（`AHAN_RIG_WORK`）：不可复用，这正是本次重拓扑要解决的问题所在。
3. **`AHAN_Blink`动作里"仅作为占位、不产生眼睑形变"的现状**：新拓扑如果按照决策1做几何表情路线、预留了眼周边线环，这个动作占位应该在新绑骨阶段被替换成真正带眼睑Shape Key的版本，不需要保留"占位不生效"这个状态。

---

## 四、验收测试动作推荐

新拓扑+新蒙皮完成后，建议直接把rig-v01已有的21个动作数值重新应用到新骨架上做验收，不需要重新设计测试动作。按覆盖的形变极限程度排优先级：

**优先级最高（覆盖各关节最大形变量，最容易暴露拓扑问题）**：
- `AHAN_Combing_PreTest`：肩60°+肘48°+腕43°，是全部动作里手臂三关节同时接近最大值的一次，最适合作为"手臂拓扑压力测试"
- `AHAN_Sit_Test`：髋25°+膝38°，膝部最大弯曲测试
- `AHAN_HandsUp`：肩58°双臂同时测试，检查双侧对称性

**优先级中等（验证常规使用范围，即产品实际会用到的日常动作）**：
- `AHAN_Wave_Left/Right`、`AHAN_RaiseHand_Left/Right`、`AHAN_Point_Left/Right`：手臂中等幅度动作
- `AHAN_Walk_Test`、`AHAN_Step_Left/Right`：腿部日常行走范围
- `AHAN_Head_Left/Right`、`AHAN_Nod`、`AHAN_ShakeHead`：头颈全部动作类型覆盖

**优先级较低（幅度很小，主要验证待机状态自然度）**：
- `AHAN_Idle`、`AHAN_Breathe`、`AHAN_Thinking`、`AHAN_Happy`、`AHAN_Bow`

---

## 五、与此前方案文档的坐标差异汇总表（需要据此更新后续重拓扑执行细节）

| 关节 | 此前方案估算 | rig-v01实测值 | 差异 | 处理建议 |
|---|---|---|---|---|
| 肩 | 70%高度 | **54%高度** | 偏高16个百分点 | 采用rig-v01数值 |
| 肘 | 52%高度 | **42%高度** | 偏高10个百分点 | 采用rig-v01数值 |
| 腕 | 58.3%高度 | **31%高度** | 偏高约27个百分点，此前明显估算错误 | 采用rig-v01数值 |
| 髋（功能性） | 28%高度 | 25%高度 | 基本一致 | 可沿用此前估算 |
| 膝（功能性） | 14%高度 | 16%高度 | 基本一致 | 可沿用此前估算 |
| 颈 | 86%高度（脚本占位值） | **63%高度** | 偏高23个百分点，此前是未经验证的占位数值 | 采用rig-v01数值 |

---

## 六、结论

本阶段只做分析，未修改 `rig-v01` 中任何文件，未开始正式重拓扑。核心结论：

1. rig-v01提供的关节坐标（尤其是肩、肘、腕、颈）比此前方案文档里的几何猜测更可靠，**手腕和颈部此前的估算存在明显偏差，已在本报告中修正**，髋和膝的此前估算基本准确，可以沿用。
2. 21个动作反推出的实际转动范围（肩60°、肘48°、腕43°、髋30°、膝38°、颈36°）应作为新拓扑设计各关节Edge Loop密度和过渡区宽度的依据。
3. 耳朵、头发、尾巴的骨骼位置合理可沿用，但从未被独立驱动过，如果不打算做二级摆动动画，新拓扑不需要为它们单独设计复杂边线环。
4. 骨架命名、层级、IK/FK设计、坐标位置、动作数值均建议继承；旧的碎片网格本身和"世界坐标包络"权重补丁方案应该废弃。
5. 21个既有动作可以直接作为新模型完成后的验收测试集，无需重新设计。

下一步：结合本报告的关节坐标和形变范围数据，确定Instant Meshes自动打底的具体测试方式，以及8个动画关键区域半手工重建的具体执行方案。
