目标：在韩国语管理端新增"巩固中心管理"模块的只读覆盖矩阵页面，按真实课程树展示每章巩固内容
覆盖状态。本任务只做展示，不做生成/预览/发布操作（那是后续 P4 任务）。

背景文档：docs/korean-practice-center-integration-roadmap.md 第7.1节（字段列表和状态取值都在
文档里，先读文档，不要复述给我）。

已完成的前置依赖（可直接使用，不要重新设计）：
- 数据表 `chapter_practice_units` / `chapter_practice_blocks` 已通过迁移
  `supabase/migrations/202608190015_chapter_practice_content.sql` 建立（尚未应用到远程环境，
  本任务只需按 schema 读取，不需要处理这一点）。
- 学生端目录已经有一版读取 `courses → lessons → course_chapters` 真实课程树的查询实现，参考
  `src/lib/course-practice-catalog.server.ts`（刚由另一个任务写的），管理端可以复用其中的课程树
  查询模式，但要新建管理端自己的查询（管理端需要看到未发布内容和全部状态，学生端查询受 RLS
  限制于已发布内容，不能直接复用同一个函数）。

已冻结的决策：
- 页面路径放在现有韩语管理端目录结构下（自己去找管理端现有"练习工具"模块的路由/权限写法，
  保持一致的目录结构、权限校验方式、UI 框架用法）。
- 权限：仅平台负责人可访问（文档第13节），复用项目里现成的平台负责人权限校验方式，不要发明
  新的角色判断。
- 每章矩阵行至少展示文档7.1列出的信息：课程/章节发布状态、教材是否发布、核心词汇数量、核心
  语法数量、听说读写词汇语法六项练习是否齐全、作业是否发布、章节测试是否发布、巩固内容是否
  生成、巩固内容版本、巩固内容状态、上次同步时间、是否因来源变化需要更新。
- 巩固内容相关字段直接读 `chapter_practice_units`（当前表为空是预期状态，所有章节应显示"未生成"，
  这不是 bug，生成逻辑是后续任务）。
- "是否因来源变化需要更新"本任务只需要能展示 `chapter_practice_units.status = 'needs_update'`
  这个已有状态值，不需要自己实现变化检测算法（检测逻辑本任务不做）。
- 遵守项目文案规范（AGENTS.md）：不要引入装饰性英文栏目标签/eyebrow/typeLabel；状态不能只靠
  颜色表达，要有文字和图标。

不要做的事（non-goals）：
- 不实现巩固包生成、编辑、预览、发布操作。
- 不实现学生练习数据看板（7.4，完成率/平均掌握度等，留给后续，因为进度还没入库）。
- 不修改数据库表结构或 RLS。
- 不修改学生端任何文件。

验收标准：
- 页面能正确展示当前数据库里真实的课程/章节（用只读查询验证行数和已知课程/章节吻合，报告中
  给出实际查询结果）。
- 非平台负责人角色访问被正确拒绝（用项目现成的权限测试方式验证，不能只看代码）。
- 类型检查/构建通过（用项目里的 typecheck / build 脚本）。
- 巩固内容字段全部显示"未生成"状态（因为表为空），不报错、不崩溃。

请按结构化报告格式回复：变更文件列表、每条验收标准 PASS/FAIL/BLOCKED、实际执行的验证命令与
输出、任何假设或风险。
