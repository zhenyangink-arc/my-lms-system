# Packet: Round1/Packet2e — 直接修改历史迁移004（用户已明确授权例外）

## 背景
Round1/Packet2d 已证实：004 (`202608190004_fix_listening_backfill_trigger_order.sql`)
在重放时会在自身执行时失败（要求章节 `status='published'`，但1—16章目前均为 `draft`），
任何时间戳晚于004的新迁移都来不及在004之前生效，无法通过"只加后置迁移"解决。

**用户已明确授权本次例外**：允许直接编辑004文件本身，放宽/移除其中的
`published` 状态限制，恢复更早（001）语义——允许draft状态章节的听力活动
参与作业听力回填。这是本方案范围内唯一被授权直接修改的历史迁移文件；
除004外，任何其他历史迁移仍然不可修改。

## 目标
1. 直接编辑 `supabase/migrations/202608190004_fix_listening_backfill_trigger_order.sql`，
   移除或放宽其中要求 `chapter/version/textbook.status = 'published'` 的条件，
   使draft状态章节的听力活动也能被判定为"可用的真实听力活动"。
2. 修改后，从干净空库执行 `npx supabase db reset --local` 必须完整跑通全部迁移
   （含023、024），退出码0。
3. 重新验证此前被阻断的两个断言，给出真实数据库查询证据：
   - 期中卷质检从"3项缺口"变为"0项缺口"。
   - 非管理会话查询不到 `EX-K1-MID-V1` 母卷和题目（RLS不可见性）。
4. 确认没有业务代码/测试依赖旧的 `published` 限制（Packet2d已初步排查未发现，
   请复核并在报告中确认）。
5. 运行既有全量测试、typecheck、lint，确认修改未引入回归。

## 非目标
- 除004外不修改任何其他历史迁移文件。
- 不修改教材发布审核状态机、不改变学生端可见性RLS规则（回填是内部函数，
  不等同于对外读取权限）。
- 不修改本方案期中卷内容合同或023/024迁移的题目内容。

## 验收标准
1. `git diff -- supabase/migrations/202608190004_*.sql` 显示改动且仅限该文件的
   状态限制条件（不做无关重写）。
2. `npx supabase db reset --local` 从空库完整跑通，退出码0，023/024均已应用。
3. 期中卷质检与RLS断言均有真实查询证据（非静态推断）。
4. 既有测试、typecheck、lint 全部通过。
5. `git status` 范围可控：004、（如需要）测试或文档更新，不含无关文件。

## 交付形式（报告）
- 004的具体改动diff摘要。
- 每条验收标准 PASS/FAIL/BLOCKED。
- 验证命令、退出码、关键输出。
- 风险说明：明确指出这是唯一被授权修改的历史迁移及原因。
