# Packet: Round5/Packet14b — 诊断007/008平台负责人阻断（不要直接修）

## 背景
Packet14报告：`npx supabase db reset --local`从空库停在
`202608190007_seed_korean_chapter_one_pilot_papers.sql`，报错"生成第一章试运行
试卷前需要一名有效的平台负责人"。这现在是整个结课方案（迁移202608200001—008）
能否通过标准Supabase迁移流程正确注册和部署的唯一阻塞点。

方案文档硬性原则：不改写已经部署的历史迁移（唯一已获用户授权的例外是Round1时的
`202608190004`，原因是该文件自身逻辑缺陷导致重放必然失败）。

## 目标（只诊断，不要写任何文件）
1. 确认"平台负责人"数据本应由哪个迁移写入、该迁移的时间戳、以及为什么它的时间戳
   晚于007（是设计如此、是错误顺序、还是本该在部署时由外部脚本/人工创建而不是迁移）。
2. 判断可行修复路径，至少给出以下几种的可行性结论：
   a) 插入一个时间戳早于007、晚于006的全新迁移，仅负责种入一个引导用的平台负责人
      账号（不修改007/008/已有的平台负责人迁移文件本身）——是否可行、是否会与后续
      已有的"平台负责人"迁移产生冲突（如唯一约束、重复创建）。
   b) 是否必须像004一样直接编辑007或008本身才能解决——如果是，说明具体原因。
   c) 是否这本来就是"新环境部署时必须先跑一次初始化脚本创建平台负责人，migrations
      本身不负责这件事"的既有设计，即当前判定为"迁移阻断"其实是缺少标准部署文档/
      脚本，而不是迁移本身有bug。
3. 明确这个问题的影响范围：只影响全新空库场景，还是也影响已有部署环境的正常升级
   （已有部署环境大概率已经有平台负责人，可能不受影响，需要证实）。

## 非目标
- 不修改任何文件，包括不新增迁移。
- 不尝试绕过或强行标记007/008为已完成。

## 交付形式（报告）
- 根因说明（含具体迁移文件路径和行号引用）。
- 至少两种可行修复方案及各自风险，附推荐方案。
- 明确回答：这是否需要用户像004那样授权修改历史迁移文件。
- 对已部署环境影响范围的结论。
