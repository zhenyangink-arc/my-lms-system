# Packet: Round5/Packet14 — 响应式、可访问性、安全与部署收尾

## 背景
方案文档第22（原文Packet14）、25、26节。这是本方案的收尾Packet，覆盖全部14个Packet
交付的功能：期中/期末考试、结课政策与资格计算、批量刷新、证书体系、机构审核、学生
结课页面、补学补考连接、结课统计。目标是把方案第26节"总体验收标准"逐条落实检查，
而不是重新实现功能。

已知的一个未闭合事项（来自Packet12c报告）：迁移`202608200007_completion_retake_connections.sql`
此前是用`psql`直接执行验证的，`supabase_migrations.schema_migrations`账本未登记。
本Packet应确认所有本方案新增迁移（202608200001—202608200008）都能通过标准
Supabase迁移流程正确登记和重放，不能只停留在手工psql验证。

## 目标
1. 手机(375px)、平板(768px)、桌面视口完整检查本方案新增的全部页面
   （学生结课页面、机构审核页面、统计面板）：无横向滚动、触控区域≥44px。
2. 键盘操作、焦点管理、对比度、打印布局逐页检查（复用已有axe或等效工具，若仓库已有
   无障碍检查脚本请复用，不要重新发明）。
3. 跨角色权限回归测试：学生/老师/机构/平台四类角色对本方案全部新表新RPC的权限边界
   （汇总此前各Packet已做过的验证，做一次端到端串联回归，而不是重新设计）。
4. 确认全部新迁移（202608200001—202608200008）能通过`npx supabase db reset --local`
   或标准迁移流程完整重放并正确登记版本号（如果仍卡在既有的007/008迁移种子数据问题，
   如实报告，不强行绕过，那是方案外的既有缺陷——已在Round1记录）。
5. 类型检查、Lint、测试套件、生产构建（`npm run build`）全部通过。
6. 确认所有深链都到达真实最终页面（复用Packet12已验证的5类深链，做一次最终确认）。

## 非目标
- 不改变本方案已实现的任何业务逻辑、算法或UI设计（只做检查和必要的修复）。
- 不涉及本方案范围外的问题（如007/008既有种子数据缺口，除非它阻断了本方案自身迁移）。

## 验收标准（对齐方案第26节）
1. 期中期末母卷齐全性、结课资格、证书三大类验收标准逐项确认为PASS（可引用此前
   各Packet已产出的证据，不必逐项重新生产，但要重新跑一次关键的自动化测试全集）。
2. 权限与数据边界：平台/机构/老师/学生权限清晰；机构隔离；未发布数据不泄露；
   已发布母卷和证书快照不可静默改写；聚合查询无明显瀑布——逐项确认。
3. 体验：今日学习显示真实任务；未达标学生知道下一步；待批改不显示为提交失败；
   手机端无横向滚动；状态同时用图标和文字；证书支持清晰打印。
4. `npm run build`生产构建成功。
5. `git status`范围可控（允许包含修复性改动，但需说明每处改动对应哪条验收项）。

## 交付形式（报告）
- 逐条对照方案第26节全部checklist项给出PASS/FAIL/BLOCKED。
- 本Packet实际做的修复（如有），文件列表。
- 验证命令、退出码。
- 全局已知风险清单（含此前各Round遗留的、仍未解决的方案外问题，如007/008
  种子数据缺口、退回重做流程评语隐藏的产品权衡等）。
- 部署建议（迁移登记顺序、是否需要种子默认政策等）。
